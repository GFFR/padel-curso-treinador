# Ingestion scripts

Server-side ingestion pipeline for course PDFs lives here from Milestone 4 onward:
PDF text extraction, theme mapping, AI candidate generation, validation, and DB import.
See `../../docs/ingestion-pipeline.md` and `docs/implementation/PLAN.md` at the repo root.
